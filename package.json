{
    "name": "AlphaGameBot",
    "private": true,
    "workspaces": [
        "bot",
        "webui"
    ],
    "scripts": {
        "bootstrap": "npm install && npm workspaces run build",
        "build": "npm workspaces run build",
        "test": "npm workspaces run test",
        "lint": "npm workspaces run lint"
    }
}